#!/usr/bin/env python3
import re
import sys
import os
import subprocess
import atexit
import signal
import time
import warnings
warnings.filterwarnings("ignore")

try:
    from sploitkit import *
    from tinyscript.helpers import is_bool, ExpiringDict, Path
    from tinyscript import *
except ImportError as e:
    print(f"Import error: {e}")
    sys.exit(1)

class OsifConsole(FrameworkConsole):
    exclude = ["root/test", "root/help"]  # Fixed typo
    sources = {'banners': "banners"}

__all__ = ["OsifConsole"]
__script__ = "osif"
__doc__ = """
Osif launcher script.
"""

# ============================================================================
# Web Server Background Process Management
# ============================================================================

class WebServerManager:
    """Manages the web server as a background subprocess"""
    
    def __init__(self):
        self.process = None
        self.web_server_path = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'web_server.py')
        self.python_path = sys.executable
        
    def start(self):
        """Start the web server in background"""
        if self.process and self.process.poll() is None:
            print("⚠️  Web server is already running")
            return False
            
        if not os.path.exists(self.web_server_path):
            print(f"⚠️  Web server not found at: {self.web_server_path}")
            return False
        
        try:
            print("🌐 Starting web server in background...")
            
            # Start web server as subprocess
            self.process = subprocess.Popen(
                [self.python_path, self.web_server_path],
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                stdin=subprocess.DEVNULL,
                cwd=os.path.dirname(self.web_server_path),
                start_new_session=True  # Detach from parent process
            )
            
            # Wait a moment to ensure it started
            time.sleep(2)
            
            if self.process.poll() is None:
                print(f"✅ Web server started (PID: {self.process.pid})")
                print("🔗 Access at: http://localhost:5001")
                return True
            else:
                print("❌ Web server failed to start")
                return False
                
        except Exception as e:
            print(f"❌ Failed to start web server: {e}")
            return False
    
    def stop(self):
        """Stop the web server subprocess"""
        if self.process and self.process.poll() is None:
            print("\n🛑 Stopping web server...")
            try:
                # Send SIGTERM for graceful shutdown
                self.process.terminate()
                try:
                    self.process.wait(timeout=5)
                    print("✅ Web server stopped")
                except subprocess.TimeoutExpired:
                    print("⚠️  Force killing web server...")
                    self.process.kill()
                    self.process.wait()
            except Exception as e:
                print(f"❌ Error stopping web server: {e}")
        self.process = None
    
    def is_running(self):
        """Check if web server is running"""
        return self.process and self.process.poll() is None

# Global web server manager
web_server = WebServerManager()

def cleanup_web_server():
    """Cleanup function called on exit"""
    web_server.stop()

atexit.register(cleanup_web_server)

def handle_signal(sig, frame):
    """Handle interrupt signals"""
    print("\n🛑 Shutting down...")
    web_server.stop()
    sys.exit(0)

signal.signal(signal.SIGINT, handle_signal)
signal.signal(signal.SIGTERM, handle_signal)

# ============================================================================
# Environment and Main Function
# ============================================================================

def initialize_env():
    env_path = os.path.join(os.path.dirname(__file__), ".env")
    if not os.path.exists(env_path):
        print("No .env file found. Let's set up your API keys.")
        keys = [
            "VT_API",
            "CENSYS_APPID",
            "CENSYS_SECRET",
            "ABUSECH_API_KEY",
            "SHODAN_API_KEY",
            "HUNTER_API_KEY",
            "BITCOINABUSE_API_KEY",
            "WIGLE_API_NAME",
            "WIGLE_API_TOKEN"
        ]
        with open(env_path, "w") as env_file:
            for key in keys:
                value = input(f"Enter value for {key}: ")
                env_file.write(f'{key}="{value}"\n')
        print(".env file created successfully!")
    else:
        print(".env file already exists. Skipping setup.")

def main():
    import argparse
    parser = argparse.ArgumentParser(description="OSIF launcher script")
    parser.add_argument("-d", "--dev", action="store_true", help="enable development mode")
    parser.add_argument("-v", "--verbose", action="store_true", help="enable verbose debug mode")
    parser.add_argument("--no-web", action="store_true", help="disable web server background process")
    args = parser.parse_args()

    initialize(exit_at_interrupt=False)

    # Add traceback
    sys.tracebacklimit = 0

    # Disable .pyc files
    sys.dont_write_bytecode = True

    # Start web server in background (unless disabled)
    if not args.no_web:
        web_server.start()
        print("=" * 60)

    c = OsifConsole(
        "OSIF",
        banner_section_styles={'title': {'fgcolor': "lolcat"}},
        dev=args.dev,
        debug=args.verbose,
    )
    
    try:
        c.start()
    finally:
        # Ensure cleanup on exit
        web_server.stop()

if __name__ == '__main__':
    initialize_env()
    main()